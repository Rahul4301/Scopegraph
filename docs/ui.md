# Memory Explorer UI

The UI is scheduled for Phase 6. It will expose a scope tree, graph view, node inspector, provenance, revision history, prune preview, reversible correction controls, and retrieval traces. The UI will call explicit API schemas and will never accept arbitrary Cypher.

