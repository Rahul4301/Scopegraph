seed: 42
retrieval:
  top_k: 8
  token_budget: 1500
  max_graph_hops: 2
  max_expanded_nodes: 40
models:
  temperature: 0.0
  max_output_tokens: 300
system: scopegraph
datasets:
  - longmemeval
  - locomo
  - memoryagentbench
runs:
  repetitions: 1
output_root: results/raw
